<?php

function inputPixDTO($data)
{

    // echo $data['amount'];
    // echo '<br>';
    // $data['amount'] = formatCurrency($data['amount'], 'BRL', false);
    // echo '<br>';
    // echo $data['amount'];
    return $data;
}